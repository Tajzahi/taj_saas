[build]
  command = "pnpm --filter @taj-saas/customer build"
  publish = "apps/customer/.next"
  ignore = "git diff --quiet $NETLIFY_PREVIOUS_COMMIT $NETLIFY_COMMIT_REF -- apps/customer/ packages/ lib/ turbo.json package.json netlify.toml"

[build.environment]
  NODE_VERSION = "20"
  PNPM_FLAGS = "--no-frozen-lockfile"
  SECRETS_SCAN_ENABLED = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
